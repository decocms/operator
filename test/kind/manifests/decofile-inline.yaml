apiVersion: deco.sites/v1alpha1
kind: Decofile
metadata:
  name: decofile-test-main
  namespace: sites-test
  labels:
    app: test
    test: e2e
spec:
  source: inline
  inline:
    value:
      config.json:
        environment: "kind-test"
        feature: "inline-source"
        timestamp: "initial"
      data.json:
        message: "Hello from inline 666"
        version: "1.0"
