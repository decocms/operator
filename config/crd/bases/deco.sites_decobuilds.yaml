---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: decobuilds.deco.sites
spec:
  group: deco.sites
  names:
    kind: DecoBuild
    listKind: DecoBuildList
    plural: decobuilds
    singular: decobuild
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.site
      name: Site
      type: string
    - jsonPath: .spec.commitSha
      name: Commit
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DecoBuild is the Schema for the decobuilds API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DecoBuildSpec defines the desired state of a Cloudflare Workers
              build.
            properties:
              branchRef:
                description: BranchRef is the branch name used as the preview alias
                  message (non-production only).
                type: string
              commitSha:
                description: CommitSha is the git commit SHA to build.
                type: string
              compatDate:
                description: CompatDate is the Cloudflare compatibility date. Defaults
                  to 2025-04-01.
                type: string
              entryPoint:
                description: EntryPoint is the worker entry file path. Defaults to
                  src/worker-entry.ts.
                type: string
              githubToken:
                description: |-
                  GithubToken is a GitHub token for cloning private repositories.
                  Prefer GithubTokenSecret for production; this field is for convenience.
                type: string
              githubTokenSecret:
                description: |-
                  GithubTokenSecret is the name of a K8s Secret (in this namespace) containing
                  a "token" key with a GitHub token. Takes precedence over GithubToken.
                type: string
              owner:
                description: Owner is the GitHub repository owner/org.
                type: string
              production:
                description: |-
                  Production indicates whether this is a production deploy.
                  When false, a wrangler preview alias is created instead.
                type: boolean
              repo:
                description: Repo is the GitHub repository name.
                type: string
              site:
                description: Site is the deco site name (used as the Cloudflare Worker
                  name by default).
                type: string
              workerName:
                description: WorkerName overrides the Cloudflare Worker name. Defaults
                  to site name.
                type: string
            required:
            - commitSha
            - owner
            - repo
            - site
            type: object
          status:
            description: DecoBuildStatus defines the observed state of a DecoBuild.
            properties:
              completionTime:
                description: CompletionTime is when the build job finished (succeeded
                  or failed).
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest observations of the build
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              jobName:
                description: JobName is the K8s Job created for this build.
                type: string
              phase:
                description: Phase is the current lifecycle phase.
                type: string
              startTime:
                description: StartTime is when the build job was created.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
