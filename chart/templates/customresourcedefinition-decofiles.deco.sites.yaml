apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: decofiles.deco.sites
spec:
  group: deco.sites
  names:
    kind: Decofile
    listKind: DecofileList
    plural: decofiles
    singular: decofile
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Decofile is the Schema for the decofiles API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DecofileSpec defines the desired state of Decofile.
            properties:
              github:
                description: GitHub contains repository information (used when source=github)
                properties:
                  commit:
                    description: Commit is the commit SHA or ref to fetch
                    type: string
                  org:
                    description: Org is the GitHub organization or user
                    type: string
                  path:
                    description: Path is the directory path within the repository
                    type: string
                  repo:
                    description: Repo is the repository name
                    type: string
                  secret:
                    description: |-
                      Secret is the name of the Kubernetes secret containing GitHub credentials.
                      If omitted, the GITHUB_TOKEN environment variable will be used.
                    type: string
                required:
                - commit
                - org
                - path
                - repo
                type: object
              inline:
                description: Inline contains direct JSON values (used when source=inline)
                properties:
                  value:
                    additionalProperties:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    description: |-
                      Value is a map where each key becomes a ConfigMap key,
                      and each value is a JSON object that will be stringified
                    type: object
                required:
                - value
                type: object
              source:
                description: Source specifies where to get the configuration data
                enum:
                - inline
                - github
                type: string
            required:
            - source
            type: object
          status:
            description: DecofileStatus defines the observed state of Decofile.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the Decofile's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configMapName:
                description: ConfigMapName is the name of the ConfigMap created for
                  this Decofile
                type: string
              githubCommit:
                description: GitHubCommit stores the commit SHA if using GitHub source
                type: string
              lastUpdated:
                description: LastUpdated is the timestamp of the last update
                format: date-time
                type: string
              sourceType:
                description: SourceType indicates which source was used (inline or
                  github)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}